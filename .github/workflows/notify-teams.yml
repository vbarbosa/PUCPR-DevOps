name: Notify Teams (simple)

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send simple JSON to Teams Flow
        run: |
          curl -H 'Content-Type: application/json' \
               -d "{\"message\": \"🚀 Commit em $GITHUB_REPOSITORY por $GITHUB_ACTOR\nCommit: $GITHUB_SHA\"}" \
               ${{ secrets.TEAMS_WEBHOOK_URL }}
