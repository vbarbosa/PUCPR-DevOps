name: Notify Teams (simple)

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send simple JSON
        run: |
          curl -v -H 'Content-Type: application/json' \
               -d '{"text": "🚀 Commit em '"$GITHUB_REPOSITORY"' por '"$GITHUB_ACTOR"' - Hash: '"$GITHUB_SHA"'"}' \
               ${{ secrets.TEAMS_WEBHOOK_URL }}
